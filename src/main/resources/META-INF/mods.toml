modLoader="javafml"

loaderVersion="[31,)"
 
issueTrackerURL="http://my.issue.tracker/"

[[mods]]

modId="packediceplus"

version="1.0.2"

displayName="Packed Ice +"

updateJSONURL="https://github.com/Stygan/Packed-Ice-Plus"

logoFile="icon.png"

credits="FML and the bois Shrek & Blair"

authors="Stygan"

# The description text for the mod (multi line!) (#mandatory)
description='''
Hello there, this is a simple mod. It adds variants to packed ice blocks.

Thank you for trying it out!
'''

[[dependencies.packediceplus]]
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[31,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"

[[dependencies.packediceplus]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.15.2]"
    ordering="NONE"
    side="BOTH"
